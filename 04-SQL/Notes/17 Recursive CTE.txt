╭─────────────────────────────────────╮
│      	🌟📥 RECURSIVE CTE🌟      	 │
╰─────────────────────────────────────╯

A Recursive CTE is a CTE that uses its own result again and again to generate or find related rows until there are no more rows to process.

It is mainly used for hierarchical data, such as:

🔸Employee → Manager → Manager's Manager
🔸Parent → Child → Grandchild
🔸Folder → Subfolder
🔸Organization hierarchy




🧠 Easy way to remember

Recursive CTE = CTE that keeps calling itself until there are no more rows to process.



💡A Recursive CTE has three elements💡

1️⃣ Non-Recursive Term:

It is a CTE query definition that forms the base result set of the CTE structure.



2️⃣Recursive Term:

One or more CTE query definitions joined with non-recursive term using UNION or UNION ALL operator.



3️⃣Termination Check:

The Recursion stops when no rows are returned from the previous iteration.






📍Syntax:

WITH RECURSIVE cte_name AS
(
    -- 1️⃣ Starting point (Non-Recursive Term / Base Query)
    SELECT ...			← CTE Query Definition

    UNION ALL

    -- 2️⃣ Repeat using previous result (Recursive Term)
    SELECT ...
    FROM table
    JOIN cte_name ...
)
SELECT *
FROM cte_name;






🔖Example:


WITH RECURSIVE my_cte AS
(
    # Base Query
    SELECT 1 AS n		
    
    UNION ALL
    
    # recursive query
    SELECT n+1 FROM my_cte
    
    # condition check
    WHERE n < 5
)
SELECT * FROM my_cte;



🔖Output:

n

1
2
3
4
5






👉USE CASES OF RECURSIVE CTE


🔹Count up until 10 without function/methods
🔹Finding Bosses & Hierarchical Level for all employees
🔹Finding Routes between Cities
🔹Finding Ancestors






🔖COMPLETE REAL WORLD EXAMPLE


📍Create Database
CREATE DATABASE recursiveCTEdb;

# Use Database
USE recursiveCTEdb;


📍Create Table
CREATE TABLE employees
(
	emp_id SERIAL PRIMARY KEY,
    emp_name VARCHAR(50),
    manager_id INT
);


📍View the table
SELECT * FROM employees;



📍 Insert the values
INSERT INTO employees
(emp_id, emp_name, manager_id)
VALUES
(1, "Arshad", Null),
(2, "Aatif", 1),
(3, "Ashu", 2),
(4, "Abhijeet", 6),
(5, "Om", 4),
(6, "Vishwa", 1),
(7, "Raazweer", 5);



📍View the table
SELECT * FROM employees;





📍 Recursive CTE

WITH RECURSIVE empCTE AS
(
	# Base Query
    SELECT emp_id, emp_name, manager_id
    FROM employees
    WHERE emp_id = 7
    
    UNION ALL
    
    	# Recursive Query
    SELECT employees.emp_id, employees.emp_name, employees.manager_id
    FROM employees
    JOIN empCTE
    ON employees.emp_id = empCTE.manager_id
)
SELECT * FROM empCTE;
    
    


👉Output:

emp_id		emp_name		manager_id

7			Raazweer			5
5			Om				    4
4			Abhijeet			6
6			Vishwa				1
1			Arshad				Null
    